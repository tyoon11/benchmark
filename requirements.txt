# =============================================================
# ECG Downstream Benchmark — Python dependencies
# =============================================================
#   pip install -r requirements.txt
#
# clinical_ts subset이 src/external/ 안에 bundled 되어있어 외부 repo
# 다운로드 불필요. 이 파일의 pip 패키지만 있으면 8개 인코더 import 가능.
# =============================================================

# ─── 핵심 ML/data ───
torch>=2.0
numpy
pandas
scipy
scikit-learn
h5py
pyyaml
tqdm

# ─── bundled clinical_ts 의 transitive 의존성 ───
einops                  # ST-MEM, S4 modules의 rearrange/repeat
timm                    # ECG-JEPA의 DropPath
opt-einsum              # CPC S4 module의 contraction
pytorch-lightning       # CPC S4Model이 LightningModule 상속
omegaconf               # CPC checkpoint pickle이 OmegaConf 타입 포함
hydra-core              # CPC load_model_from_config (선택)
fastcore                # ECGFM-KED의 fastcore.foundation 사용
fastai                  # ECGFM-KED의 fastai.layers / fastai.core
safetensors             # HuBERT-ECG checkpoint 포맷
transformers            # HuBERT-ECG의 HuBERT backbone
biosppy                 # HuBERT-ECG preprocessing의 bandpass filter
