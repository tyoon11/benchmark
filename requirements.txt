# ===========================================================================
# ECG Downstream Benchmark — pip dependencies
# ===========================================================================
#   pip install -r requirements.txt
#
# The `clinical_ts` subset is bundled in `src/external/`, so no external repo
# clone is needed. The packages below are sufficient to import the 8 reference
# encoder adapters and run the benchmark.
# ===========================================================================

# ── Core ML / data ──────────────────────────────────────────────────────────
torch>=2.0
numpy>=1.24
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
h5py>=3.9
pyyaml>=6.0
tqdm>=4.66

# ── Transitive dependencies of the bundled clinical_ts subset ───────────────
einops>=0.7              # ST-MEM, S4 modules (rearrange / repeat)
timm>=0.9                # ECG-JEPA, MERL ViT (DropPath)
opt-einsum>=3.3          # CPC S4 module contraction
pytorch-lightning>=2.0   # CPC S4Model subclasses LightningModule
omegaconf>=2.3           # CPC checkpoint pickle contains OmegaConf types
fastcore>=1.5            # ECGFM-KED uses fastcore.foundation
fastai>=2.7              # ECGFM-KED uses fastai.layers / fastai.core
ipython>=8.0             # fastprogress (fastai dep) imports IPython.display directly
safetensors>=0.4         # HuBERT-ECG checkpoint format
transformers>=4.30       # HuBERT-ECG backbone
biosppy>=2.1             # HuBERT-ECG preprocessing bandpass filter

# ── Optional ─────────────────────────────────────────────────────────────────
# hydra-core>=1.3        # only used if you build configs via Hydra
