# =============================================================
# ECG Downstream Benchmark — Python dependencies
# =============================================================
# 이 파일은 `tykim/benchmark/`을 실행하는 데 필요한 패키지를 정리합니다.
# 같은 stack을 hbkim env에서 쓰려면 아래 명령으로 설치하세요:
#
#   /home/irteam/local-node-d/_conda/envs/hbkim/bin/pip install -r requirements.txt
#
# (이미 만족된 패키지는 자동으로 skip됩니다.)
#
# 환경별 현황 (2026-04-28 기준):
#   tykim env: 아래 패키지 전부 ✓ (벤치마크 기존 동작 환경)
#   hbkim env: einops, hydra-core 만 누락
# =============================================================

# ─── 핵심 ML/data ───
torch>=2.0
numpy
pandas
scipy
scikit-learn
h5py
pyyaml
tqdm

# ─── ECG-FM benchmark adapter 의존성 ───
# clinical_ts (ecg-fm-benchmarking/code) 가 transitively 요구하는 라이브러리들.
# 한 번이라도 인코더 import 하면 필요해진다.
einops                  # ST-MEM, S4 modules의 rearrange/repeat
timm                    # ECG-JEPA의 DropPath
pytorch-lightning       # CPC S4Model이 LightningModule 상속
omegaconf               # CPC checkpoint pickle이 OmegaConf 타입 포함
hydra-core              # CPC load_model_from_config (선택; 직접 weight 로드 시 불필요)
fastcore                # ECGFM-KED의 fastcore.foundation 사용
safetensors             # HuBERT-ECG checkpoint 포맷
transformers            # HuBERT-ECG의 HuBERT backbone
