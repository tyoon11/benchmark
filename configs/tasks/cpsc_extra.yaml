# cpsc_extra (32-class multi-label, paper)
data:
  # original: df[df.data_length >= 5000] (main_lite_ecg.setup_dataset)
  min_data_length: 5000
  batch_size: 64
  h5_root: ${ECG_DATA_ROOT}/h5/physionet/v2.0
  label_csv: labels/cpsc_extra_paper_labels.csv
  normalize: false
  num_workers: 8
  table_csv: ${ECG_DATA_ROOT}/h5/physionet/v2.0/cpsc_extra_table.csv
task:
  name: cpsc_extra
  num_classes: 32
  expected_num_classes: 33   # main_lite_ecg.py
