# georgia (50-class multi-label, paper)
data:
  batch_size: 64
  h5_root: ${ECG_DATA_ROOT}/h5/physionet/v2.0
  label_csv: labels/georgia_paper_labels.csv
  normalize: false
  num_workers: 8
  table_csv: ${ECG_DATA_ROOT}/h5/physionet/v2.0/georgia_table.csv
  target_fs: 500
  target_length: 5000
task:
  name: georgia
  num_classes: 50
